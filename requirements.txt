# NOTE: numpy/pandas are deliberately NOT capped at <2 / <3. Streamlit
# Community Cloud has (as of writing) a known bug where runtime.txt is
# ignored and the build always uses a newer Python than requested, which
# in turn has no installable wheel for numpy<2 -- causing "Oh no. Error
# running app." with no useful traceback. This app's code is tested and
# working against both the old (numpy 1.26 / pandas 2.x) and new
# (numpy 2.x / pandas 3.x) dependency lines, so leave these unpinned.
streamlit>=1.34,<2
numpy>=1.26
pandas>=2.2
matplotlib>=3.8,<4
seaborn>=0.13,<1
scipy>=1.11,<2
statsmodels>=0.14,<1
networkx>=3.0,<4
plotly>=5.18,<8
